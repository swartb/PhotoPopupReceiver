<Window x:Class="PhotoPopupReceiver.TextPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="360" Height="200"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">
    <!--
        Outer container with rounded corners and a semi-transparent dark background.
        AllowsTransparency="True" on the Window combined with Background="Transparent"
        allows this Border's CornerRadius to produce a truly rounded-corner popup
        without any square window chrome being visible.
        The #EE prefix gives 93% opacity so underlying content is subtly visible.
    -->
    <Border CornerRadius="14" Background="#EE1E1E1E" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <!-- Row 0: text content takes all available vertical space. -->
                <RowDefinition Height="*" />
                <!-- Row 1: action buttons sized to their content. -->
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Text display area with a slightly lighter background for contrast. -->
            <Border CornerRadius="10" Background="#FF2A2A2A" Padding="10">
                <!-- Read-only TextBox so the user can select and copy text freely. -->
                <TextBox x:Name="MessageTextBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         Background="Transparent"
                         Foreground="White"
                         BorderThickness="0"
                         FontSize="14"
                         VerticalScrollBarVisibility="Auto"
                         Cursor="IBeam"/>
            </Border>

            <!-- Action buttons aligned to the right, below the text area. -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <!-- Copies the displayed text to the Windows clipboard. -->
                <Button x:Name="CopyButton" Padding="14,6" Margin="0,0,8,0" Click="Copy_Click"/>
                <!-- Dismisses and closes the popup window. -->
                <Button x:Name="CloseButton" Padding="14,6" Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
