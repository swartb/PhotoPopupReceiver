using System.Configuration;
using System.Data;
using System.Windows;

namespace PhotoPopupReceiver
{
    /// <summary>
    /// Entry point for the WPF application.
    /// This partial class pairs with <c>App.xaml</c>, which declares the startup URI
    /// (<see cref="MainWindow"/>) and any application-wide resources.
    /// Custom application-level logic (e.g. global exception handling or startup tasks)
    /// can be added here by overriding <see cref="Application.OnStartup"/> or handling
    /// the <see cref="Application.Startup"/> event.
    /// </summary>
    public partial class App : Application
    {
    }

}
